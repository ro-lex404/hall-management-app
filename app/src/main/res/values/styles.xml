<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#424242</item>
        <item name="colorPrimaryDark">#424242</item>
        <item name="colorAccent">#FFC107</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Custom style for the RatingBar -->
    <style name="YellowRatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="colorControlNormal">#BDBDBD</item> <!-- A light grey for the empty stars -->
        <item name="colorControlActivated">@color/colorAccent</item> <!-- Yellow for the filled stars -->
    </style>

    <!-- Custom theme for the Navigation Drawer -->
    <style name="NavigationDrawerTheme" parent="ThemeOverlay.MaterialComponents.Dark">
        <item name="android:textColorSecondary">#BDBDBD</item> <!-- Light grey for sub-headers -->
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/colorPrimaryDark</item>
    </style>

</resources>
